generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url = env("postgresql://neondb_owner:npg_bewm4TMW5Gri@ep-frosty-surf-amyq5vs6.c-5.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require")

}

model Listing {
  id          Int      @id @default(autoincrement())
  title       String
  price       Float
  description String
  images      String[]
  createdAt   DateTime @default(now())
}